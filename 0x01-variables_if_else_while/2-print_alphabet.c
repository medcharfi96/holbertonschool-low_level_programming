#include <stdio.h>
/**
 * main fonction: see all the alphabet
 * putchar is allowd
 * Return: if 0 (mean true)
 */
int main(void)
{
char caractere;
for (caractere = 'a'; caractere <= 'z'; caractere ++>
putchar(caractere);
putchar('\n');
return (0);
}

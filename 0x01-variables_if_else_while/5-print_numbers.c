#include <stdio.h>
/**
 * main - Entry point
 * show all number that are <10
 * Return: if 0 (mean true)
 */
int main(void)
{
int x;
for (x = 0; x < 10; x++)
printf("%d", x);
printf("\n");
return (0);
}
